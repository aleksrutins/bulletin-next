import { Abril_Fatface, Besley } from "next/font/google";

export const abril = Abril_Fatface({weight: '400', subsets: ['latin', 'latin-ext']});
export const besley = Besley({ subsets: ['latin', 'latin-ext'] });